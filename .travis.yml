services:
  - docker
language: bash
install:
  - echo "install nothing!"
script:
  - echo "no tests!"
deploy:
  script:
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
    - docker build -t serious-comp/couchbase-server-sandbox .
    - docker push serious-comp/couchbase-server-sandbox
